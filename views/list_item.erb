<!DOCTYPE html>
<html>
<head>
<style>
    table {
        border-collapse: collapse; 
        border:1px solid #000000;
        font-size: 20px;
        font-family: 'Helvetica'
    } 
    table td{
        border:1px solid #000000;
        padding:5px;
    }
    table thead tr td{
        border:1px solid #000000;
        background-color: 	#53aff4;
        padding:5px;
    }
</style>
</head>
<body>

<h1> List of Item </h1>
<table>
<thead>
    <tr>
        <td> Name </td>
        <td> Price </td>
        <td> </td>
        <td> </td>
        <td> </td>
    </tr>
</thead>
<tbody>
    <% items.each do |item| %>
        <tr>
            <td><%= item.name %></td>
            <td>Rp. <%= item.price %></td>
            <td> <%= item.id %>
            <td> <a href="/items/detail/<%= item.id %>"> Show </a> </td>
            <td> <a href="/items/update/<%= item.id %>"> Update </a> </td>
            <td> <a href="/items/delete/<%= item.id %>"> Delete </a> </td>
        </tr>
    <% end %>
</tbody>
</table>
<br/> 
<a href="/items/create" style="font-size: 30px">New Item</a>